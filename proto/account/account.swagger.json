{
  "swagger": "2.0",
  "info": {
    "title": "proto/account/account.proto",
    "version": "version not set"
  },
  "schemes": [
    "http",
    "https"
  ],
  "consumes": [
    "application/json"
  ],
  "produces": [
    "application/json"
  ],
  "paths": {
    "/account/message/email": {
      "post": {
        "summary": "邮箱发送验证码",
        "operationId": "SendEmailCode",
        "responses": {
          "200": {
            "description": "A successful response.",
            "schema": {
              "$ref": "#/definitions/accountSendMesaageResponse"
            }
          }
        },
        "parameters": [
          {
            "name": "body",
            "in": "body",
            "required": true,
            "schema": {
              "$ref": "#/definitions/accountEmailOrPhoneRequest"
            }
          }
        ],
        "tags": [
          "Account"
        ]
      }
    },
    "/account/validate/code/email": {
      "post": {
        "summary": "邮箱验证校验",
        "operationId": "ValidateEmailCode",
        "responses": {
          "200": {
            "description": "A successful response.",
            "schema": {
              "$ref": "#/definitions/accountValidataCodeResponse"
            }
          }
        },
        "parameters": [
          {
            "name": "body",
            "in": "body",
            "required": true,
            "schema": {
              "$ref": "#/definitions/accountEmailOrPhoneRequest"
            }
          }
        ],
        "tags": [
          "Account"
        ]
      }
    }
  },
  "definitions": {
    "accountEmailOrPhoneRequest": {
      "type": "object",
      "properties": {
        "email": {
          "type": "string"
        },
        "phone": {
          "type": "string"
        }
      },
      "title": "邮件或手机号"
    },
    "accountSendMesaageResponse": {
      "type": "object",
      "properties": {
        "status": {
          "type": "string",
          "format": "int64"
        },
        "errmsg": {
          "type": "string"
        },
        "token": {
          "$ref": "#/definitions/accountToken"
        }
      },
      "title": "邮箱或者手机号发送验证码的结果"
    },
    "accountToken": {
      "type": "object",
      "properties": {
        "email_token": {
          "type": "string"
        },
        "phone_token": {
          "type": "string"
        }
      },
      "title": "通过token去缓存找对应的手机号或者邮箱"
    },
    "accountUserBaseInfo": {
      "type": "object",
      "properties": {
        "id": {
          "type": "string",
          "format": "int64",
          "title": "id"
        },
        "email": {
          "type": "string",
          "title": "邮箱"
        },
        "phone": {
          "type": "string",
          "title": "手机号"
        },
        "name": {
          "type": "string",
          "title": "用户的真实姓名"
        }
      },
      "title": "数据库,基本的账户信息"
    },
    "accountValidataCodeResponse": {
      "type": "object",
      "properties": {
        "status": {
          "type": "string",
          "format": "int64"
        },
        "errmsg": {
          "type": "string"
        }
      },
      "title": "code校验结果"
    }
  }
}
